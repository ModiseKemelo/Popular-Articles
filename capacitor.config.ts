import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.modisekemelo.assment',
  appName: 'PopularArticles',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
